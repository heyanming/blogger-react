// Define the entry point for the module
import { request } from "./request";
import { getToken, setToken, removeToken } from "./token";
// Export the module
export { request, getToken, setToken, removeToken };
